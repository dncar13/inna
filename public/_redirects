# Netlify _redirects file for SPA routing
# This ensures React Router works properly on refresh

/*    /index.html   200

# API redirects (if needed in the future)
/api/*  https://your-api-domain.com/api/:splat  200

# Redirect old URLs to new structure (add if needed)
# /old-page  /new-page  301
